/**
 * App bundle aggregator — imported once at startup so every business app's
 * registerAppView() runs before the platform renders any mount point. Add one
 * import line per app.
 */
import './roundtable';

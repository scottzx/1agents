// Re-export shim — moved to core/services (Phase 0 carve). Import from
// '@1agents/core/services/workspaceService' directly in new code; this preserves existing importers.
export * from '@1agents/core/services/workspaceService';

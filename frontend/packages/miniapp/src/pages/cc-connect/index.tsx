// 渠道 sub-page (from 更多) — cc-connect channels, the no-path cc-connect surface.
import { CcConnectWebView } from '../../components/CcConnectWebView';

export default function CcConnect() {
  return <CcConnectWebView titleKey="more.ccConnect" />;
}

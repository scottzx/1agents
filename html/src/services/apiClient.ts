// Re-export shim — moved to core/services (Phase 0 carve). Import from
// '../core/services/apiClient' directly in new code; this preserves existing importers.
export * from '../core/services/apiClient';
